# ================================
# Docker Compose for Development
# Hot reload enabled
# ================================

services:
  # ==================== DEV APPLICATION ====================
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: komikstream-dev
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://komikstream:komikstream_secret@db:5432/komikstream
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-}
      - CLERK_WEBHOOK_SECRET=${CLERK_WEBHOOK_SECRET:-}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-https://api.sansekai.my.id/api}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - komikstream-network

  # ==================== DATABASE ====================
  db:
    image: postgres:16-alpine
    container_name: komikstream-db-dev
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=komikstream
      - POSTGRES_PASSWORD=komikstream_secret
      - POSTGRES_DB=komikstream
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    networks:
      - komikstream-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U komikstream -d komikstream"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

# ==================== VOLUMES ====================
volumes:
  postgres_dev_data:
    driver: local

# ==================== NETWORKS ====================
networks:
  komikstream-network:
    driver: bridge
